# 根 .gitignore —— Imgo2 工作区（单一仓库）
# 原则：跟踪训练与部署真正需要的源码、模型、动作数据和文档，排除可再生成的产物。
# 仓库包含约 100 MB 的 STL 网格，这是有意保留的：四份模型副本各被一处流程引用
# （原始模型、采集模型、训练用副本、部署副本），去掉网格会让 URDF 无法加载。
#
# 本文件合并了原先 imgo2_rl/ 与 imgo2_deploy/ 各自的 .gitignore。合并时把只对
# 某个子树成立的规则加了路径前缀，避免在整仓范围内过度匹配。

# ---------- Python ----------
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/
*.so
*.pb
*_pb2.py

# ---------- 日志与训练输出 ----------
logs/
**/logs/
*.log
*.log.*
**/wandb/
**/.neptune/
**/output/
**/outputs/
**/videos/
**/recordings/
**/runs/
**/generated/
**/docs/_build/

# ---------- 检查点 ----------
# 训练 checkpoint 体积大且可再生成，不入库。
# 例外：imgo2_deploy/policy/imgo2/himloco/himloco.pt 是部署配置引用的占位策略，
# 体积不到 1 MB，需要保留。
*.pt
!imgo2_deploy/policy/imgo2/himloco/himloco.pt
*.pth
*.ckpt
*.onnx
*.jit

# ---------- C++ / ROS 构建产物 ----------
cmake_build/
**/cmake-build*/
devel/
install/
*.o
*.a
MUJOCO_LOG.TXT

# 仅对部署子树成立的构建/下载产物
/imgo2_deploy/log/
/imgo2_deploy/.catkin_tools/
# 部署包的 package.xml 由构建生成（源文件是 package.ros1.xml / package.ros2.xml）
/imgo2_deploy/**/package.xml
/imgo2_deploy/rust_build/
/imgo2_deploy/Cargo.lock
/imgo2_deploy/**/onnxruntime/
/imgo2_deploy/**/libtorch/
/imgo2_deploy/**/mujoco/
/imgo2_deploy/**/.cache/
/imgo2_deploy/**/rl_sar_zoo/

# ---------- Isaac Sim / Omniverse（原 imgo2_rl/.gitignore 的规则） ----------
**/*.usda
**/*.usdc
**/*.usdz
**/*.dmp
**/.thumbs/
/imgo2_rl/_isaac_sim*
/imgo2_rl/_repo
/imgo2_rl/_build
/imgo2_rl/.lastformat
/imgo2_rl/.isaac-lab-docker-history
**/.isaac-lab-docker-history
/imgo2_rl/.pretrained_checkpoints/
/imgo2_rl/docker/cluster/exports/
/imgo2_rl/docker/.container.cfg
/imgo2_rl/docker/artifacts/
**/*.sif

# ---------- 压缩包与临时目录 ----------
*.zip
*.tgz
*.tar.gz
*.rar
temp_extract*/
*.tmp

# ---------- 编辑器与系统 ----------
.vscode/
.idea/
.DS_Store
Thumbs.db

# ---------- 本地历史备份（不属于仓库内容） ----------
# 合并嵌套仓库前的 Imgo2 / imgo2_rl 历史 bundle，保存在本机供回滚，
# 不应提交，也不需要同步到服务器。
.git-backups/

# ---------- 明确保留（曾经被误忽略，勿再启用） ----------
# imgo2_rl/datasets/imgo2_motion/ 是 AMP 训练实际加载的 21 份参考动作；
# imgo2_rl/tests/ 含 AMP 修复文件集与提交训练前的前置检查。
# 详见 docs/amp_alignment_review.md。
