<?xml version="1.0"?>
<!-- ament 包（ROS 2）。原先这里是 format 2 + catkin 的 ROS 1 包描述，2026-09-17 为
     让 Gazebo/ros2_control 链路能 colcon 构建而改成 ament；ROS 1 分支仍在 CMakeLists 里。
     归属沿用参考项目 imgo2_description（qmq-h / Apache-2.0）。 -->
<package format="3">
    <name>imgo2_description</name>
    <version>2.0.0</version>
    <description>
        Imgo2 模型唯一源：xacro 内核与可选模块、生成物 URDF、MuJoCo 模型（mjcf）、
        Gazebo/ros2_control 控制器配置与网格。模型物理参数以训练侧为准（README MODEL-01/02）。
    </description>
    <maintainer email="qmq@gmail.com">qmq-h</maintainer>
    <license>Apache-2.0</license>

    <buildtool_depend>ament_cmake</buildtool_depend>

    <depend>urdf</depend>
    <depend>xacro</depend>
    <depend>robot_state_publisher</depend>

    <exec_depend>gazebo_ros</exec_depend>
    <exec_depend>gazebo_ros2_control</exec_depend>
    <exec_depend>controller_manager</exec_depend>

    <export>
        <build_type>ament_cmake</build_type>
        <gazebo_ros gazebo_model_path="${prefix}/.."/>
    </export>
</package>
