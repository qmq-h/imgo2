# 24500 轮 AMP 的足端步态回放（Isaac Lab，2026-09-18）

对象：`imgo2_rl/logs/amp_rsl_rl/base_move_amp/2026-09-17_21-25-57/model_24500.pt`（`iter=24500`，
sha256 `cc8ee22a…`，即 2026-09-18 接入部署的那一份）。
工具：[`imgo2_rl/scripts/tools/eval_gait.py`](../imgo2_rl/scripts/tools/eval_gait.py)（回放）
+ [`gait_metrics.py`](../imgo2_rl/scripts/tools/gait_metrics.py)（口径，纯 numpy）。
判读用尺子：[`docs/gait_reference_baseline.json`](gait_reference_baseline.json)。

这份记录回答的是「**Gazebo 侧的结论在训练侧能否复现，且足端层面到底差在哪**」。
Gazebo 侧（[记录 9.7 节](gazebo_ros2_bringup_2026-09-17.md)）给的是关节信号派生的周期强度/相位；
本记录给的是**接触时序与足端行程**，两者互相独立。

---

## 1. 结论摘要

三次回放（v1 无 warmup；v2 有 warmup；**v3 = 修复堆叠轴/相位后的当前版本**）里，
下表用 **v3**：8 环境 × 950 步、丢弃前 50 步、指令固定 `vx=1.0 m/s`。

| 项 | v3 实测（vx=1.0） | 参考基线 | 判定 |
|---|---|---|---|
| 基座高度 | **0.3117 ± 0.0036 m** | 目标 0.30 m | ✅ 达标 |
| 线速度误差 | **0.1078 m/s**（89% 跟踪） | — | ✅ 可用（见 §3.4） |
| 角速度误差 | **0.0928 rad/s** | — | ✅ 可用 |
| 步态周期 | **0.1926 s（5.19 Hz）** | 0.600 s（1.67 Hz） | ❌ **快 3.11 倍**（唯一硬缺陷） |
| 步幅 x（base 系峰峰） | **0.139–0.160 m**（均 0.149） | 1.0 m/s 档均 **0.295** | ⚠️ 约 0.5 倍，但口径偏软（§3.1 ①） |
| 抬脚高度 z | **0.067–0.085 m**（均 0.073） | 0.083 / 0.112 / 0.121 | ✅ 同量级、略低 |
| **相位（trot）** | **FL-FR +174.2°、FL-RR +17.3°（对角线同相）** | trot 应 ±180° | ✅ **干净对角步** |
| 相位集中度 R | **0.86–0.91** | — | ✅ 落地很规整 |
| 关节峰峰值 | shank **0.516–0.618 rad**、thigh 0.407–0.470、hip 0.110–0.180 | shank 中位 **0.635**（范围 0.477–1.063）、thigh 中位 **0.477** | ✅ **落在参考范围内** |
| 前/后腿对称性 | 前腿 0.145、后腿 0.153（后/前 **1.06**） | 前进档 0.87–0.96、后退档 1.01–1.06 | ✅ 正常散布 |
| 机身 pitch | 均值 **+2.15°**（恒定抬头）、RMS 0.81°、峰峰 6.22° | 前进档 ≈**−0.5°** | ❌ 恒定抬头、方向与参考相反 |
| 机身 roll | 均值 +0.18°、RMS 0.88° | 均值 ≈0、RMS 0.33° | ✅ 基本达标 |
| yaw | 峰峰 24.08°，累积漂移率 **+0.328 °/s** | — | ⚠️ 以摆动为主，漂移很小 |

`vx=0.6 m/s`（也在数据覆盖内）对照：周期 **0.1980 s（5.05 Hz）**、步幅 0.111–0.146、
抬脚 0.049–0.062、占空比升到 **0.59–0.71**、shank 峰峰值降到 0.378–0.459、相位仍是对角
（FL-FR +176.0°、集中度 0.67–0.77）、pitch +2.87°。
⇒ **降速时接触时序变保守（占空比↑、关节幅度↓），但步频仍然 3 倍** ⇒ 后面要问的
「是不是指令超出数据覆盖」得到回答：**不是**。

**修完口径后的结论（与 v1/v2 差异很大，逐条更正见 §4.6–§4.8）**：
策略实际是一条**干净、规整、左右对称的对角步（trot）**，关节幅度、抬脚高度、相位都对得上
录制参考；**唯一实质缺陷是步频 3.1 倍**（外加一个恒定的抬头 2.1–2.9°）。
之前几版报的「高抬腿 1.6 倍」「前后腿不对称 1.33」「shank 幅度 1.35–2.25 倍」全部是
**未 warmup 的出生瞬态污染 + 一处堆叠轴 bug**，已撤回。

## 2. 运行条件

```bash
cd <repo>/imgo2_rl
python scripts/tools/eval_gait.py \
    --task=Imgo2-basemove-flat-amp-play \
    --checkpoint=logs/amp_rsl_rl/base_move_amp/2026-09-17_21-25-57/model_24500.pt \
    --num_envs=8 --steps=1000 --warmup=50 \
    --out ../docs/gait_eval_amp_24500.json --headless
```

* 解释器：`/opt/conda/envs/isaaclab/bin/python`（本机训练机，Isaac Lab + Isaac Sim 5.0）。
* play 配置把指令固定为 **`vx = 1.0 m/s`、`vy = 0`、`yaw = 0`**（`amp_env_cfg.py:237-239`），
  并且关掉了域随机化与参考状态初始化（`Imgo2AmpMovePlayEnvCfg`），所以这是「同一条指令的纯策略回放」。
* `step_dt = 0.02 s`，8 环境 × 1000 步 = 每环境 20 s。
* v1（下表数值的来源）**没有** `--warmup`；`--warmup=50` 是本节修订后的默认值，用于丢掉出生
  0.35 m → 稳态 0.31 m 的下落瞬态。

---

## 3. 结果

### 3.1 逐足指标（v3：`--warmup=50`；vx=1.0）

| 足 | 占空比 | 步频 Hz | 步幅 x (m) | 抬脚 z (m) | 相位 vs FL | 集中度 R |
|---|---|---|---|---|---|---|
| FL_FOOT | 0.563 | 5.191 | 0.150 | 0.067 | 0（参考） | — |
| FR_FOOT | 0.604 | 5.151 | 0.139 | 0.068 | **+174.2°** | 0.91 |
| RL_FOOT | 0.522 | 5.171 | 0.145 | 0.072 | **+193.3°** | 0.89 |
| RR_FOOT | 0.496 | 5.171 | 0.160 | 0.085 | **+17.3°** | 0.86 |

① **相位是干净的 trot**：FL↔RR 同相（17.3°）、FR↔RL 同相（174.2° vs 193.3°），
偏离理想 ±180° 约 13–17°，集中度 0.86–0.91 ⇒ 每步落地都很规整。
这与 Gazebo 侧同一 checkpoint 测到的 **FL-FR 176.9°** 在 3° 以内一致 ——
v2 报的 155.9°/0.17 是相位估计器被非整数周期抹平造成的假象（§4.8）。

② **步幅口径要留余地**：`foot_stride_x_m` 取的是足端在 base 系 x 的峰峰值，它 =
支撑期位移 + 摆动期前摆（含过冲）+ **机身俯仰摆动经 base 系耦合进来的分量**。
本策略 pitch 峰峰 6.2°、足端离基座约 0.3 m ⇒ 光俯仰就能贡献约 0.03 m（占实测 0.15 的 20%），
而录制动作为 0.01 m 量级。另外用「无滑移」关系估算（支撑期位移 = v·占空比·周期 =
0.892×0.563×0.1926 = 0.097 m）也比实测 0.150 m 小 ⇒ **该指标系统性偏软**，
「步幅只有参考 0.5 倍」应读作「明显偏小」，不要当精确倍数用。

③ 关节峰峰值（v3，顺序 = `cfg.joint_names`：每腿 hip/thigh/shank）：

| 腿 | hip | thigh | shank |
|---|---|---|---|
| FL | 0.125 | 0.407 | 0.580 |
| FR | 0.110 | 0.418 | 0.543 |
| RL | 0.180 | 0.432 | 0.516 |
| RR | 0.171 | 0.470 | 0.618 |

对照参考（19 份行走动作）：shank 峰峰值中位 **0.635**、范围 **0.477–1.063**；
thigh 中位 **0.477**。⇒ **策略的关节幅度落在参考范围内**（shank 略低于中位）。

### 3.2 与参考的逐腿对照（v3）

| 档 | FL | FR | RL | RR | 均值 | 后/前 |
|---|---|---|---|---|---|---|
| forward_0.3 | 0.117 | 0.130 | 0.113 | 0.102 | 0.115 | 0.87 |
| forward_0.6 | 0.215 | 0.182 | 0.184 | 0.189 | 0.193 | 0.94 |
| forward_0.9 | 0.268 | 0.282 | 0.262 | 0.251 | 0.266 | 0.93 |
| forward_1.2 | 0.366 | 0.352 | 0.328 | 0.365 | 0.353 | 0.96 |
| backward_0.6/0.9/1.2 | — | — | — | — | 0.174/0.287/0.388 | 1.06/1.03/1.01 |
| **策略 vx=1.0** | 0.150 | 0.139 | 0.145 | 0.160 | 0.149 | **1.06** |
| **策略 vx=0.6** | 0.129 | 0.111 | 0.128 | 0.146 | 0.129 | **1.13** |

参考的前后腿差只有 4–13%（方向随行进方向翻转，符合运动学直觉）；策略在 1.06/1.13，
**落在参考的正常散布内 ⇒ 之前 v1 报的「1.33、方向相反」已撤回**（出生瞬态污染，§4.6）。
口径：参考数据第 19–30 列就是足端 base 系 xyz，与 `eval_gait.py` 的 `foot_pos_b` 同口径；
`docs/gait_reference_baseline.json` 里的 `foot_stride_x_m` 只取 FL 一条腿，故上表同时给均值。
逐腿数据与口径说明已写回该 JSON 的 `per_leg_stride_x_m`。

### 3.3 与远端 Gazebo 记录（9.7 节）的对照

| 结论 | Gazebo（部署链路，每命令一条新栈） | Isaac Lab（训练侧回放 v3） | 是否一致 |
|---|---|---|---|
| 速度跟踪 @1.0 m/s | 99.8% | 89%（误差 0.108 m/s） | ⚠️ 方向一致，数值差约 10 个百分点 |
| 站立/低速 | 已达标（0.2→93.4%、0.3→97.9%） | 0.6 m/s：占空比升到 0.59–0.71、关节幅度下降 | ✅ 低速行为一致 |
| 步频偏高 | 约 2.5 倍（周期 0.208–0.365 s） | **3.11 倍（0.1926 s）** | ✅ 一致（本侧把倍数钉得更死） |
| **相位/对角步** | FL-FR **176.9°**（trot） | FL-FR **+174.2°**、FL-RR +17.3°、R 0.86–0.91 | ✅ **差 3° 以内，两个仿真器互相印证** |
| 姿态 | roll 3.5–6.0°、yaw ≤1°/s | roll 0.18–0.24°、pitch 恒定 +2.09°、yaw 净漂移 0.33°/s | ⚠️ 本侧 roll 更好，但多出恒定抬头 |

跟踪率的差异**不是矛盾**：两个仿真器的接触求解器/时间步不同，且 Gazebo 侧是在加速段之后
取稳态窗口、本侧是含启动段的 19 s 平均。**v3 之后训练侧与部署侧在「步态形态」上已经对齐**
（都是干净的 ±180° 对角步、都只有步频偏高），这也是第一次两个独立仿真器在步态结论上完全一致。

## 4. 本轮修掉的测量缺陷（**v1 JSON 中相关字段作废**，共 4 处 + 1 处加载健壮性）

### 4.1 逐环境占空比索引错轴（已修 + 测试锁定）

`per_foot.*.contact_air_fraction_per_env` 原写成 `1 − c[:, e].mean()`，而 `c` 是 `[E, T]`：
`c[:, e]` 取的是**第 e 个时刻、全部环境**的一列，不是第 e 个环境的时间序列。于是它给出的
是「某一时刻有几成环境腾空」＝ k/8 的离散值，v1 里出现 `1.0 / 0.0 / 0.125 / 0.375` 这种
数值，看起来像「7/8 个环境的 RL 足整段不落地」——**那是索引 bug，不是策略行为**。
正确写法是 `c[e].mean()`，且必须满足自洽条件
`mean(逐环境空中占比) == 全样本时间加权空中占比`；现在这条自洽性由
`build_report()` 里的断言强制，并有单元测试覆盖。

**同一份数据里聚合量（占空比/步频/空中支撑时长）是正确的**（它们一直按时间加权算），
所以 §3.1 的数值仍然可用；只有那一列明细作废。

### 4.2 v1 的相位是「单环境 + 首个落地事件」估计，不可用；新版改圆周平均

v1 相位只取 **0 号环境**每只足的**第一个**落地事件，再按周期取模。在周期 0.193 s、`dt = 0.02 s`
下，一个采样点就是 0.1 个周期，加上落地抖动，单次事件无法代表整段；v1 给出
FL/FR/RL/RR = 0.00/0.80/0.41/0.71（FR−FL ≈ 288°），与 Gazebo 同 checkpoint 的 **176.9°** 不符。
改成「**全部落地事件 × 全部环境**对各自环境参考足栅格做圆周平均」，并同时输出集中度 R
（越接近 1 越规整），与 `dt` 无关。另外新版把 **FL-FR 相位（trot 判据）** 和 `gait_period_s`
直接打进摘要与 JSON。

> 修订过程中新版相位一度写成「周期乘 `dt` 换成秒、再与以步为单位的事件下标取模」——
> 合成 trot 立刻算出 0° 而不是 180°，被新增的单测抓住后修掉。这也说明**相位不能只有一次
> 实跑证据**：两次都是靠合成信号才发现的。

### 4.3 `episode_length_mean_steps = 499.5` 是斜坡均值，不含重置信息

`episode_length_buf` 在没有重置时就是 `0…T−1`，它的时间均值恒为 `(T−1)/2 = 499.5`。
v1 把它当成「情节长度约 500 步」来读是错的。判断有没有被中途重置要看 **done 计数**与
**末步缓冲值**：v2 记录 `resets_total` / `resets_per_env` / `episode_length_final_steps_*`
并在摘要里打印。v1 因此**无法确认**回放期间是否发生过终止重置（峰峰类指标是否被重置打断）。

### 4.4 旧字段名 `shank_peak_to_peak_rad` 其实含全部 12 个关节

v1 打印里 12 个值都被标成 shank。v2 增加 `joint_peak_to_peak_rad`（按腿展开），
真正的 shank 是每腿第 3 个（`leg2/5/8/11`）；旧字段名保留但值指向同一份数据。

### 4.5 顺带修的一处 checkpoint 加载健壮性（`map_location`）

`AMPOnPolicyRunner.load()` 原来是 `torch.load(path, weights_only=...)`，**没有 `map_location`**：
在 GPU 机上等价于按存档设备号恢复（自洽），但（a）在另一块 GPU 号上存的 checkpoint 会载不进来，
（b）CPU-only 机器直接报
`Attempting to deserialize object on a CUDA device but torch.cuda.is_available() is False`。
已改成 `map_location=self.device`。**验证边界**：写这一处是为了在本机沙箱做 CPU 冒烟测试，
它确实让加载越过了这一步（随后卡在 PhysX，见 §5 第 1 条）；**GPU 路径按构造是恒等映射（同设备重映射），
但没有在 GPU 上回归过** ⇒ 仍是「已修，待验证」。PPO/HIM runner 有同样写法，本次**未动**。

### 4.6 抬脚高度与前/后腿不对称：v1 数值被出生瞬态污染（**结论已更正**）

v1 没有 `--warmup`，把「出生在 0.35 m、落到稳态 0.31 m」那段也算进足端 z/x 的峰峰值：
机器人在空中时腿是默认姿态、落地后腿压缩，于是足端在 base 系的 z 峰峰值被抬到 0.17–0.18 m。
加 `--warmup=50`（1 s）后同一 checkpoint 的实测是 **0.066–0.083 m**，与参考同速档
（0.083–0.121 m）同量级甚至偏低 ⇒ **「高抬腿」这条撤回**。前/后腿比也从 1.33 降到 **1.10**
（参考前进档 0.87–0.96、后退档 1.01–1.06），属轻微、不再是主要问题。

### 4.7 关节峰峰值全为 0.000：又一次堆叠轴写错（v2 已修 + 加形状校验）

重构时把 `joint_pos` 用 `np.stack(..., axis=1)` 堆成 `[E,T,12]`，而 `build_report` 按 `[T,E,12]`
取 `[:, :, i]`，于是峰峰值算成了「同一时刻跨环境的散布」≈ 0，12 个关节全打印 0.000。
其余指标恰好都是 `axis=0` 堆叠，看起来完全正常，极易漏过。修法：
① 统一成「**时间轴一律在 0**」并由 `_expect()` 严格校验形状，写反直接报错；
② 新增 `test_transposed_input_is_rejected` 等单测。**因此 §1 的 shank/thigh 峰峰值需下次回放重测。**

### 4.8 相位估计器：改用「插值到相邻两个参考落地之间」

v2 的相位仍是取模法：集中度只有 **0.15–0.17**（FL-FR 156°），而 Gazebo 侧同一 checkpoint
是稳定的 ±180°。根因是真实步周期不是整数个仿真步（0.1927 s ÷ 0.02 s = 9.6 步），取模会把
每周期的 0.4 步误差累积上百个周期后抹平圆周平均。已改为把每个落地事件插值到
「相邻两个参考足落地」的区间上（对周期漂移免疫），并用合成漂移周期单测锁定。
**相位与集中度需下次回放重测。**

### 4.9 新增的离线回归

`imgo2_rl/tests/test_gait_metrics.py`（18 项，纯 numpy，无需 GPU）：逐环境聚合顺序、
游程覆盖全时长、落地事件、相位（含半周期 180°、同相 0°、脏事件不被单点带偏）、
峰峰值/逐环境均值、姿态欧拉角与「恒定倾斜」判定、yaw 漂移率区分漂移/摆动、
以及 `build_report()` 的端到端汇总（warmup 切窗、重置计数、时间序列长度）。
`cd imgo2_rl && python3 -m unittest discover -s tests -p test_gait_metrics.py` → **18 通过**；
全仓测试 27 项通过。

---

## 5. 待修复 / 待确认（**缺什么才能完成**）

> 详细的证据、判据与优先级见单独的 [AMP 步态调整方案](amp_gait_adjust_plan_2026-09-18.md)。
> 这里只留结论与「缺什么」。

| # | 事项 | 状态 | 缺什么 |
|---|---|---|---|
| 1 | **步频 3.1 倍**（唯一实质缺陷） | ⏳ 待改配置重训 | 两条候选：**P1 降 `λ_gp`（10→1~2）**——离线合成对照显示同预算下 λ 从 10 降到 1 会把「正常节律 vs 3 倍速节律」的风格奖励差从 0.105/步 提到 0.564/步；**P2 加 `feet_air_time`**（目标滞空 ≈0.2 s，当前 0.067–0.085 s）。判据：步周期 ≥0.45 s 且线速度误差 ≤0.6 m/s。每次需一次完整训练（≈4 h / 10000 轮） |
| 2 | **恒定抬头 +2.09°（1.0 m/s）/ +2.87°（0.6 m/s）** | ⏳ 待决定 | `flat_orientation_l2` 现在 `weight=0.0`、被 `disable_zero_weight_rewards()` 过滤掉；打开它做单变量消融即可，注意与高度项耦合。需一次训练 |
| 3 | 步幅「0.5 倍」的精确性 | ⚠️ 口径偏软 | 该指标含摆动过冲与俯仰耦合（§3.1②），要用**世界系**足端轨迹或「支撑期足端位移」重算才能给精确倍数；数据已在 `--dump-npz` 里，可离线做 |
| 4 | ~~指令超出数据覆盖~~ | ✅ 已排除 | 0.6 m/s（覆盖内）步频仍 3.0 倍 ⇒ 不是原因。压指令范围只作为部署包线治理 |
| 5 | ~~高抬腿 / 前后腿不对称 / 关节幅度过大~~ | ✅ 已撤回 | 均为瞬态污染或堆叠轴 bug（§4.6、§4.7）；v3 真值分别为 0.067–0.085 m、1.06–1.13、shank 0.52–0.62 rad |
| 6 | Isaac Lab 89% 与 Gazebo 99.8% 的跟踪差 | ⏳ 待量 | 同协议对比：本侧需要只取稳态窗口，Gazebo 侧需要给出误差定义与窗口 |
| 7 | 参考基线缺少**占空比与相位** | ⏳ 待补 | 占空比可由录制动作的足端 z 阈值离线推出（`audit_amp_dataset.py` 已能读足端位置），补上后「占空比 0.50–0.71」才有同口径判读 |

## 6. 复现与验证边界

* §1/§3 的数值来自 **v3** 两次回放（用户终端执行，`/opt/conda/envs/isaaclab/bin/python`）：
  `--command_vx=1.0` 与 `--command_vx=0.6`，各 8 环境 × 1000 步、`--warmup=50`，
  产物 `docs/gait_eval_amp_24500.json`、`docs/gait_eval_amp_24500_vx0.6.json`，
  原始数组（含策略侧 43 维 `amp_obs`）在 `imgo2_rl/logs/gait_24500_vx{0.6,1.0}.npz`（已被 .gitignore 忽略）。
* v1/v2 的 JSON 只保留结论级数值用于对照；其中**已被判定无效的字段**在 §4 逐条列出
  （逐环境占空比列、v1 的相位、`episode_length` 解读、v2 的关节峰峰值）。
* §4 的离线回归：`cd imgo2_rl && python3 -m unittest discover -s tests -p "test_*.py"` → **31 项通过**。
* 判别器分析全部**离线**（CPU）：`probe_amp_discriminator.py`（专家侧）、
  `probe_amp_policy_vs_expert.py`（策略侧，读 `--dump-npz` 产物）、`probe_gp_effect.py`（λ_gp 合成对照）。
* **沙箱内无法代跑 Isaac Lab**：Isaac Sim 的 PhysX 需要 CUDA，`--device=cpu` 也过不去
  （实测报错见下）；因此所有回放都由用户在 GPU 终端执行。
* 本轮**未**改任何训练配置、**未**重训、**未**跑真机/GUI/MuJoCo。步态结论只到「评估/诊断」这一步，
  修复方案见 [AMP 步态调整方案](amp_gait_adjust_plan_2026-09-18.md)，按工作区约定属于
  「有证据支持的建议」，不是已完成的修复。
